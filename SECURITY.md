# 安全说明

这个仓库只存放可公开分发的 Skill 代码和说明，不应包含任何真实课程数据或账号凭证。

请勿提交：

- 课程群消息、图片、实时字幕、妙记原文或成品文档内容；
- 真实 `chat_id`、`meeting_id`、消息 ID、用户 ID、文档 token 或飞书链接；
- `lark-cli` 配置、登录态、Cookie、访问令牌、密钥或私钥；
- 本机绝对路径、课程会话目录、`state.json`、日志和发布中间产物。

自动同步任务在提交前执行安全扫描。扫描失败时会保留本地修改并停止提交和推送，不会强制覆盖远端。

如果发现安全问题，请通过 GitHub 私下联系仓库维护者，不要在公开 Issue 中粘贴凭证或真实课程内容。
